on:
  pull_request:
  push:
    branches:
      - main

jobs:
  changes:
    name: Detect changed contract areas
    runs-on: core
    outputs:
      legacy: ${{ steps.filter.outputs.legacy }}
      standards: ${{ steps.filter.outputs.standards }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            legacy:
              - 'genesis/**'
              - 'tests/**'
              - 'bin/**'
              - 'scripts/setup-compiler.sh'
              - 'scripts/download-rusk.sh'
              - 'scripts/build-test-contracts.sh'
              - 'scripts/check-locked-deps.sh'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'Makefile'
              - 'README.md'
              - 'rust-toolchain.toml'
              - '.github/workflows/ci.yml'
            standards:
              - 'standards/**'
              - 'docs/dusk-contract-standards*'
              - 'scripts/dusk-contract-standards-*'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'Makefile'
              - 'README.md'
              - 'rust-toolchain.toml'
              - '.github/workflows/ci.yml'
              - '.github/workflows/standards-hardening.yml'

  dusk_analyzer:
    name: Dusk Analyzer
    runs-on: core
    steps:
      - uses: actions/checkout@v4
      - uses: dsherret/rust-toolchain-file@v1
      - run: cargo install --git https://github.com/dusk-network/cargo-dusk-analyzer
      # cargo dusk-analyzer is a custom subcommand and does not accept --locked,
      # so validate locked resolution of the root workspace up front. The root
      # toolchain cannot parse some newer host-only manifests, so filter to the
      # contract target platform.
      - run: cargo metadata --locked --format-version 1 --filter-platform wasm32-unknown-unknown > /dev/null
      - run: cargo dusk-analyzer
      - run: rustup toolchain install nightly-2026-02-27 --profile minimal --component rust-src --component rustfmt --component clippy --target wasm32-unknown-unknown
      - run: cd standards && cargo dusk-analyzer

  locked_deps:
    name: Locked dependency preflight
    needs: changes
    if: needs.changes.outputs.legacy == 'true'
    runs-on: core
    steps:
      - uses: actions/checkout@v4
      - uses: dsherret/rust-toolchain-file@v1
      - run: ./scripts/check-locked-deps.sh

  clippy:
    name: Clippy check
    needs: [changes, locked_deps]
    if: needs.changes.outputs.legacy == 'true'
    runs-on: core
    steps:
      - uses: actions/checkout@v4
      - uses: dsherret/rust-toolchain-file@v1
      - run: cargo fmt
      - run: make clippy

  test:
    name: Run Tests
    needs: [changes, locked_deps]
    if: needs.changes.outputs.legacy == 'true'
    runs-on: core
    steps:
      - uses: actions/checkout@v4
      - uses: dsherret/rust-toolchain-file@v1
      - run: make keys
      - run: make test

  standards:
    name: Standards CI
    needs: changes
    if: needs.changes.outputs.standards == 'true'
    runs-on: core
    steps:
      - uses: actions/checkout@v4
      - uses: dsherret/rust-toolchain-file@v1
      - run: rustup toolchain install nightly-2026-02-27 --profile minimal --component rust-src --component rustfmt --component clippy --target wasm32-unknown-unknown
      - run: make standards-ci
